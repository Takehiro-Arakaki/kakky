<!DOCTYPE html>
<html>
  <head>
    <title>RubyApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav>
        <% if sysadmin_signed_in? %>
          <%# <strong><%= link_to current_user.name, home_show_path</strong> %>
          <%= link_to 'プロフィール変更', edit_sysadmin_registration_path %>
          <%= link_to 'ログアウト', destroy_sysadmin_session_path, method: :delete %>
        <% else %>
          <%# <%= link_to 'サインアップ', new_sysadmin_registration_path %>
          <%# <%= link_to 'ログイン', new_sysadmin_session_path %>
        <% end %>
      </nav>
  </header>

    <%= render 'shared/notice' %>

    <%= yield %>
  </body>
</html>
