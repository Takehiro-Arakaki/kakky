<%= form_with(model: admin_question_select, local: true) do |form| %>
  <% if admin_question_select.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_question_select.errors.count, "error") %> prohibited this admin_question_select from being saved:</h2>

      <ul>
      <% admin_question_select.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.number_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="field">
    <%= form.label :is_right %>
    <%= form.check_box :is_right %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
