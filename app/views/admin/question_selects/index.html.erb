<p id="notice"><%= notice %></p>

<h1>Admin Question Selects</h1>

<table>
  <thead>
    <tr>
      <th>Question</th>
      <th>Content</th>
      <th>Is right</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @admin_question_selects.each do |admin_question_select| %>
      <tr>
        <td><%= admin_question_select.question_id %></td>
        <td><%= admin_question_select.content %></td>
        <td><%= admin_question_select.is_right %></td>
        <td><%= link_to 'Show', admin_question_select %></td>
        <td><%= link_to 'Edit', edit_admin_question_select_path(admin_question_select) %></td>
        <td><%= link_to 'Destroy', admin_question_select, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Admin Question Select', new_admin_question_select_path %>
